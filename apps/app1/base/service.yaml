apiVersion: v1
kind: Service
metadata:
  name: app1
  namespace: argocd
  labels: { app: app1 }
spec:
  selector:
    app: app1
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http